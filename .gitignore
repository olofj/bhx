/target/

# Downloaded firmware + kernel artifacts (bhx kernel pull / manual copy).
/Image
/fw_jump.bin
/blackhole-card.dtb
/blackhole-p100*.dtb

# Disk images (rootfs.ext4, images/*.ext4).
*.ext4
/images/
/ramdisks/

# fetch::download_to_cached sidecars next to downloaded artifacts.
*.fetch.json
*.fetch.json.tmp

# Daemon runtime output when --log-file points into the repo.
/daemon-card*.log

# Validation harness outputs (probe_sweep-<UTC>.csv, etc.).
/probe_sweep-*.csv

# libvdeslirp source tarballs / build tree (downloaded for linking).
/libvdeslirp-*/
/libvdeslirp_*.tar.*
/libvdeslirp_*.dsc
/libvdeslirp_*.debian.tar.*

# Claude Code session state (plans, agent notes).
/.claude/
tests/alma/
/u-boot.bin
/soak_endurance-*.csv

# Python bytecode cache (scripts/bench/lib.py is invoked as a module).
__pycache__/

# Default cidata seed path used by the README walk-through.
/seed.iso

# External checkouts kept in-tree for browsing; never committed
luwen/
tt-kmd/
