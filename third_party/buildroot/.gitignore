# Buildroot source extract + build dir + download cache. Regenerable.
buildroot-*/
output/
dl/

# Convenience symlink the Makefile maintains pointing at
# buildroot-*/output/images/rootfs.ext4. Soaks find it via this path
# but it's regenerable on `make`.
rootfs.ext4
